// Re-export the existing reusable EmptyState under the App* namespace
// so consumers can `import { EmptyState } from "@/components/ui/app"`.
export { default, default as EmptyState } from "@/components/EmptyState";